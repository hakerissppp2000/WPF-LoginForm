<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style x:Key="BorderStyle" TargetType="{x:Type Border}">    
        <Setter Property="Background">
            <Setter.Value>
                <ImageBrush ImageSource="/Images/дум тем.png" Stretch="Fill" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RamkiStyle" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#6495ED" Offset="0"/>
                    <GradientStop Color="#191970" Offset="0.75"/>
                    <GradientStop Color="#191970" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
 </Style>

    <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#6495ED"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#28AEED"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LogoStyle" TargetType="{x:Type Image}">
        <Setter Property="Source" Value="/Images/Logo2.1.png">
           
        </Setter>
    </Style>

</ResourceDictionary>
